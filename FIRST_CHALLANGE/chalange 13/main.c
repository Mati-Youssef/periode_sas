#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int num;
	printf("entre un numbre \n");
	scanf("%d",&num);
	printf("la valeur équivalente en octal et %o",num);
	printf("la valeur équivalente en hexadécimal et %x",num);
return 0;
}
